{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "calculator",
  "description": "This tool can perform simple arithmetic calculations in Python",
  "type": "object",
  "properties": {
    "tool": {
      "type": "string",
      "enum": ["calculator"]
    },
    "input": {
      "type": "string",
      "description": "Arithmetic expression parsable in pure Python. Single line only. Don't use any imports or external libraries"
    }
  },
  "required": ["tool", "input"]
}