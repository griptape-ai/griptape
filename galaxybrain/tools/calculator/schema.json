{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "calculator",
  "description": "This tool can perform simple calculations in Python",
  "type": "object",
  "properties": {
    "tool": {
      "type": "string",
      "enum": ["calculator"]
    },
    "input": {
      "type": "string",
      "description": "Calculator expression parsable in Python. Don't use any imports"
    }
  },
  "required": [
    "tool",
    "input"
  ]
}